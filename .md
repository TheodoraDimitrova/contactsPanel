Contact application it's full CRUD app.Using the Context API of React, Jsonplaceholder API(for http requests).We can add,update,delete contacts.The data doesn't got persisted if you reload the page goes away,because jsonplaceholder API doesn't save to their database!!!
